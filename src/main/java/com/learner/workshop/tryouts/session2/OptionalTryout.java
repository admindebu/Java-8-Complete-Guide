package com.learner.workshop.tryouts.session2;

/*
 * @Author Debu Paul
 */

/**
 * Tryouts for Optional
 *
 */
public class OptionalTryout {

	public static void main(String[] args) {

		// Create a optional String and initialize with "imaginea" - name

		// Create another empty Optional object - empty

		// Assert the value presence of 'name' and assert if the wrapped value
		// of empty is null

		// Get the length of the string 'name' only if it has a value

		// Create an optional object and return a default value if the wrapped
		// value is null

		// Create an optional object and return a value through Supplier
		// interface id the value is null

		// Create an optional object and throw an exception if the wrapped value
		// is null.

	}

}
